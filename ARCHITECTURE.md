# 围串标识别智能体架构设计

## 1. 总体架构

- **输入层**：读取 `docs/` 下供应商 Markdown 文件、图片与询价文件。
- **处理层**：
  1. 标题结构分块（章-节-条优先）
  2. 分块分类与表格/图片检测
  3. 单块证据提取
  4. 跨供应商相似度比对（simhash/chroma）
  5. 关联图谱构建（networkx）
- **决策层**：多专家 Agent 评分 + ChiefAuditorAgent 仲裁。
- **输出层**：Markdown 与 JSON 风险报告。

## 2. 工作流映射（对应项目需求）

### 阶段1：基于标题结构的智能分块与分类
- 使用 `RecursiveCharacterTextSplitter`，分隔符按 `#`、`##`、`###`、`####`。
- 对 chunk 应用规则分类：
  - 采购需求
  - 商务条款
  - 资质证明
  - 技术方案
  - 法律声明
  - 图片/表格

### 阶段2：多模态图片解析
- 当检测到 `![](...)` 链接时，调用 `vision_tool`。
- 目标识别：公章、签字、证书编号、图表异常。

### 阶段3：单文件解析与证据提取
- 采用审计提示词抽取：关键数据、异常特征、法律风险点、关联线索。
- 保留原始片段与提取结果用于证据链可追溯。

### 阶段4：多智能体专家团队协同分析
- 4 个专家 Agent 并行评分（0-10）。
- `ChiefAuditorAgent` 处理分歧：
  - 若最大分差 >= 4，触发二次研判。
  - 输出冲突说明与补充核查建议。

### 阶段5：综合分析报告生成
- 汇总财务、技术、行为、法律四类证据。
- 根据平均分映射风险等级：
  - 高风险（>=7）
  - 中风险（4~7）
  - 低风险（<4）

## 3. 中国采购审计专项规则

### 3.1 必备文件合规检查
- 中小企业声明函
- 无重大违法记录声明
- 授权委托书
- 无围标串标声明

### 3.2 典型围串标特征
- 投标保证金来自同一账户
- 法定代表人交叉任职
- 联系方式交叉/隐藏
- 工程量清单分项报价比例异常一致
- 技术参数非通用段落高度雷同

### 3.3 法律依据库
- 《招标投标法实施条例》第三十九条、第四十条
- 《政府采购法》第七十七条
- 《政府采购货物和服务招标投标管理办法》第三十七条

## 4. 工程实现建议

1. 检索层：以 `langchain-chroma` 建立 chunk 向量索引。
2. 元数据层：以 `pymupdf` 提取 PDF 作者、创建时间、修改时间并写入证据字段。
3. 相似度层：`simhash` 快速初筛 + 向量相似度复核。
4. 图谱层：`networkx` 构建“供应商-联系人-电话-账户-IP”关联图。
5. 审计追溯：报告中每条证据绑定 `supplier/source_file/chunk_id`。
